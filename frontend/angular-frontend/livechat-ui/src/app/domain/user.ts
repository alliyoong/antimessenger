export interface User {
  userId?: string,
  username: string,
  firstName: string,
  lastName: string,
  phone?: string,
  email: string,
  bio?: string,
  address?: string,
  createdAt?: string,
  imageUrl?: string,
  imageFile?: string,
}
